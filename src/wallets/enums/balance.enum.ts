/**
 * Wallet Type ENUM
 * To check the type of wallet
 * and update accordingly
 */

export enum WalletType {
  BALANCE = 'BALANCE',
  SENT_TRANSACTION = 'SENT_TRANSACTION',
  RECEIVED_TRANSACTION = 'RECEIVED_TRANSACTION',
}
